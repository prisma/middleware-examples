# Prisma + AWS KMS = ❤️

## Usage

### 1. Set everything up

```bash
yarn install
docker compose up -d
yarn init-vault
```

### 2. Run the code

```bash
yarn start # starts the script at src/main.ts
```
