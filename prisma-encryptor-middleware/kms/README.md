# Prisma + AWS KMS = ❤️

## Usage

### 1. Set everything up

```bash
yarn install
yarn init-database
export AWS_REGION=<region>
export KEY=<arn to aws key>
```

### 2. Run the code

```bash
yarn start # starts the script at src/main.ts
```
